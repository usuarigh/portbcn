---
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<!doctype html>
<html lang="ca">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0;url=${baseUrl}/article-1/`} />
    <link rel="canonical" href={`${baseUrl}/article-1/`} />
    <script define:vars={{ targetUrl: `${baseUrl}/article-1/` }}>
      window.location.replace(targetUrl);
    </script>
    <title>PierNext - Port de Barcelona</title>
  </head>
  <body>
    <p>Redirigint a <a href={`${baseUrl}/article-1/`}>Quan el GPS menteix</a>...</p>
  </body>
</html>
